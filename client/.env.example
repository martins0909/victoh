# Example Vite env for local development (do NOT commit secrets)
# Copy this file to `.env.local` or `.env` in the client/ folder and update values.
# Vite exposes env vars prefixed with VITE_ at build time.

VITE_API_URL=http://localhost:5002
